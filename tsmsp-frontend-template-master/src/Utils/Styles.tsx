import {StyleSheet} from "react-native";

//All style should only exist in its own utils, this page will be removed in the future
export const styles = StyleSheet.create({


    backgroundImage:{
        flex: 1,
        resizeMode: "cover",
        alignItems: 'center',
        justifyContent: "center",
    },

});