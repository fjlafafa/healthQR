import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center'
    },

    backgroundImage:{
        flex: 1,
        resizeMode: "cover",
        alignItems: 'center',
        justifyContent: "center",
    },

    text:{
        fontSize: 20,
        fontFamily: "Arial"
    }
});